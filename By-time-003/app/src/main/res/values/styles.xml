<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="styleActivity">
        <item name="android:background">@color/colorBackground
        </item>
    </style>

    <style name="styleButtonStart">
        <item name="android:textColor">@color/colorStart</item>
        <item name="android:background">@drawable/button_start</item>
    </style>

    <style name="styleText">
        <item name="android:textColor">@color/colorText
        </item>
    </style>


</resources>
